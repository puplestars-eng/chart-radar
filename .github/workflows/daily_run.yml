name: Daily Stock Analysis

# 언제 실행할까요?
on:
  schedule:
    # 한국 시간 아침 8시 (UTC 23:00)
    - cron: '0 23 * * *'
  workflow_dispatch: # 수동 실행 버튼 (테스트용)

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 저장소 체크아웃 (코드 가져오기)
      uses: actions/checkout@v3

    - name: 파이썬 설정 (3.9 버전)
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: 라이브러리 설치
      run: |
        pip install -r requirements.txt

    - name: 주식 분석 실행 (여기가 핵심!)
      # 🚨 중요: 엑셀 만드는 파일명이 radar.py가 맞나요? 아니면 수정해주세요!
      run: |
        python radar.py

    - name: 결과 저장 및 퇴근 (Commit & Push)
      run: |
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "actions@github.com"
        git add *.xlsx
        git commit -m "🤖 로봇이 데이터 업데이트 완료!" || echo "변경 사항 없음"
        git push